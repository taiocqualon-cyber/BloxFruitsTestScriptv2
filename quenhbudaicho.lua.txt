repeat task.wait() until game:IsLoaded()

-- 🟢 ICON MỞ MENU (GOLD STYLE)
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(255,215,0) -- 💛 vàng
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.1, 0, 0.2, 0)
ImageButton.Size = UDim2.new(0, 45, 0, 45)
ImageButton.Draggable = true
ImageButton.Image = "rbxassetid://15059956542"

UICorner.CornerRadius = UDim.new(1, 0)
UICorner.Parent = ImageButton

ImageButton.MouseButton1Down:Connect(function()
	game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)

-- 🟣 LOAD FLUENT
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
repeat wait() until game:IsLoaded()

-- 🪟 WINDOW GOLD HUB
local Window = Fluent:CreateWindow({
	Title = "💛 quenh lọ đế",
	SubTitle = "GOLD HUB SYSTEM 😈",
	TabWidth = 150,
	Size = UDim2.fromOffset(460, 320),
	Acrylic = true,
	Theme = "Dark",
	MinimizeKey = Enum.KeyCode.End
})

-- 💛 NOTIFY
Fluent:Notify({
	Title = "💛 quenh lọ đế",
	Content = "GOLD HUB LOADED 😈",
	Duration = 3
})

-- 📑 TAB MỚI (NGẦU HƠN)
local Tabs = {
	Home = Window:AddTab({ Title = "💎 Home" }),
	Script = Window:AddTab({ Title = "⚙️ Scripts" }),
	Info = Window:AddTab({ Title = "📢 Info" }),
	Misc = Window:AddTab({ Title = "🎮 Misc" })
}

------------------------------------------------
-- 💎 HOME
------------------------------------------------
Tabs.Home:AddParagraph({
	Title = "💛 Welcome",
	Content = "Chào mừng đến với Gold Hub – quenh lọ đế 😈"
})

Tabs.Home:AddButton({
	Title = "💛 Copy TikTok",
	Description = "Follow để nhận script mới",
	Callback = function()
		setclipboard("https://www.tiktok.com/")
	end
})

------------------------------------------------
-- ⚙️ SCRIPT
------------------------------------------------
Tabs.Script:AddButton({
	Title = "⚡ Load Speed Hub",
	Description = "Script đa năng",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua"))()
	end
})

------------------------------------------------
-- 📢 INFO
------------------------------------------------
Tabs.Info:AddParagraph({
	Title = "📊 System",
	Content = "Gold Hub Version 1.0\nStyle: Gold VIP 😈"
})

------------------------------------------------
-- 🎮 MISC
------------------------------------------------
Tabs.Misc:AddButton({
	Title = "😈 Test Notify",
	Description = "Test thông báo",
	Callback = function()
		Fluent:Notify({
			Title = "😈 GOLD HUB",
			Content = "Hoạt động ổn định 💛",
			Duration = 3
		})
	end
})

------------------------------------------------
-- 💛 CUSTOM VÀNG TOÀN UI
------------------------------------------------
task.spawn(function()
	wait(1)

	for _,v in pairs(game.CoreGui:GetDescendants()) do
		if v:IsA("TextLabel") or v:IsA("TextButton") then
			v.TextColor3 = Color3.fromRGB(255,215,0)
		end
	end
end)