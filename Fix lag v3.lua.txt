local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
local gui = Instance.new("ScreenGui", player.PlayerGui)

-- FPS LABEL
local fpsLabel = Instance.new("TextLabel", gui)
fpsLabel.Size = UDim2.new(0,120,0,40)
fpsLabel.Position = UDim2.new(0,10,0,10)
fpsLabel.BackgroundTransparency = 0.3
fpsLabel.BackgroundColor3 = Color3.fromRGB(0,0,0)
fpsLabel.TextColor3 = Color3.new(1,1,1)
fpsLabel.Font = Enum.Font.GothamBold
fpsLabel.TextScaled = true
fpsLabel.Text = "FPS: --"

-- Cập nhật FPS live
local lastUpdate = tick()
local frameCount = 0
RunService.RenderStepped:Connect(function()
	frameCount = frameCount + 1
	if tick() - lastUpdate >= 1 then
		fpsLabel.Text = "FPS: "..frameCount
		frameCount = 0
		lastUpdate = tick()
	end
end)

-- MENU TRÒN NHỎ GỌN
local menu = Instance.new("Frame", gui)
menu.Size = UDim2.new(0,50,0,50) -- menu nhỏ hơn
menu.Position = UDim2.new(0.1,0,0.5,-25) -- mặc định bên trái
menu.AnchorPoint = Vector2.new(0,0.5)
menu.BackgroundColor3 = Color3.fromRGB(0,170,255)
menu.Active = true
menu.Draggable = true
local corner = Instance.new("UICorner", menu)
corner.CornerRadius = UDim.new(1,0)

-- NÚT ON/OFF
local toggle = Instance.new("TextButton", menu)
toggle.Size = UDim2.new(1,0,1,0)
toggle.Text = "OFF"
toggle.TextColor3 = Color3.new(1,1,1)
toggle.Font = Enum.Font.GothamBold
toggle.BackgroundTransparency = 1
toggle.TextScaled = true

local running = false
toggle.MouseButton1Click:Connect(function()
	running = not running
	toggle.Text = running and "ON" or "OFF"
	if running then
		-- FIX LAG VIP
		Lighting.Brightness = Lighting.Brightness * 0.8 -- giảm 20%
		for _,v in pairs(Lighting:GetDescendants()) do
			if v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("ColorCorrectionEffect") then
				v.Enabled = false -- giảm hiệu ứng 40%
			end
		end
		print("✅ FIX LAG VIP ON: ánh sáng -20%, hiệu ứng -40%, FPS tối ưu")
	else
		-- RESET LIGHTING
		Lighting.Brightness = Lighting.Brightness / 0.8
		for _,v in pairs(Lighting:GetDescendants()) do
			if v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("ColorCorrectionEffect") then
				v.Enabled = true
			end
		end
		print("✅ FIX LAG VIP OFF: Restore ánh sáng và hiệu ứng")
	end
end)