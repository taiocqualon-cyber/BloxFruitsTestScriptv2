--[[ 
    GHOST REDZ V1 | REMAKE EDITION
    - Theme: Red & Dark (Phong cách Redz Hub)
    - Cải tiến: Bỏ hệ thống Key, Tăng tốc độ Scan Player, Fix Silent Aim 99%.
    - Chức năng: Farming, PvP, ESP, Hunting.
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer
local mouse = player:GetMouse()
local camera = workspace.CurrentCamera

-- ===== HỆ THỐNG BIẾN (STATE) =====
local Ghost_Config = {
    SilentAim = false,
    AutoClick = false,
    ESP_Box = false,
    Target = nil,
    Prediction = 0.15
}

-- ===== UI FRAMEWORK (REDZ STYLE) =====
local sg = Instance.new("ScreenGui", player.PlayerGui)
sg.Name = "GhostRedz_V1"
sg.ResetOnSpawn = false

-- Nút mở Menu (👿)
local openBtn = Instance.new("TextButton", sg)
openBtn.Size = UDim2.new(0, 55, 0, 55)
openBtn.Position = UDim2.new(0, 15, 0, 200)
openBtn.Text = "👿"
openBtn.TextSize = 35
openBtn.BackgroundColor3 = Color3.fromRGB(30, 0, 0)
openBtn.TextColor3 = Color3.fromRGB(255, 0, 0)
openBtn.Draggable = true
Instance.new("UICorner", openBtn).CornerRadius = UDim.new(1,0)
Instance.new("UIStroke", openBtn).Color = Color3.fromRGB(255, 0, 0)

-- Khung chính (Main Frame)
local main = Instance.new("Frame", sg)
main.Size = UDim2.new(0, 480, 0, 320)
main.Position = UDim2.new(0.5, -240, 0.4, -160)
main.BackgroundColor3 = Color3.fromRGB(15, 10, 10)
main.Visible = false
Instance.new("UICorner", main)
Instance.new("UIStroke", main).Color = Color3.fromRGB(200, 0, 0)

openBtn.MouseButton1Click:Connect(function() main.Visible = not main.Visible end)

-- Thanh Tab bên trái
local tabsFrame = Instance.new("Frame", main)
tabsFrame.Size = UDim2.new(0, 120, 1, -10)
tabsFrame.Position = UDim2.new(0, 5, 0, 5)
tabsFrame.BackgroundTransparency = 1

local container = Instance.new("Frame", main)
container.Size = UDim2.new(1, -135, 1, -10)
container.Position = UDim2.new(0, 130, 0, 5)
container.BackgroundTransparency = 1

-- Hàm tạo Tab (Redz Style)
local pages = {}
local function addTab(name, iconText, order)
    local btn = Instance.new("TextButton", tabsFrame)
    btn.Size = UDim2.new(1, 0, 0, 40)
    btn.Position = UDim2.new(0, 0, 0, (order-1)*45)
    btn.Text = iconText .. " " .. name
    btn.BackgroundColor3 = Color3.fromRGB(35, 10, 10)
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    Instance.new("UICorner", btn)

    local page = Instance.new("ScrollingFrame", container)
    page.Size = UDim2.new(1, 0, 1, 0)
    page.Visible = (order == 1)
    page.BackgroundTransparency = 1
    page.CanvasSize = UDim2.new(0, 0, 2, 0)
    page.ScrollBarThickness = 2
    pages[name] = page

    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(pages) do p.Visible = false end
        page.Visible = true
    end)
    return page
end

-- Hàm tạo Cần gạt (Toggle)
local function addToggle(parent, text, order, callback)
    local tgl = Instance.new("TextButton", parent)
    tgl.Size = UDim2.new(0.95, 0, 0, 40)
    tgl.Position = UDim2.new(0, 0, 0, (order-1)*45)
    tgl.Text = text .. ": OFF"
    tgl.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    tgl.TextColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", tgl)

    local s = false
    tgl.MouseButton1Click:Connect(function()
        s = not s
        tgl.Text = text .. (s and ": ON" or ": OFF")
        tgl.BackgroundColor3 = s and Color3.fromRGB(150, 0, 0) or Color3.fromRGB(30, 30, 30)
        callback(s)
    end)
end

-- ===== THIẾT LẬP CÁC TAB =====
local fTab = addTab("Farm", "⚔️", 1)
local pTab = addTab("PvP", "🎯", 2)
local vTab = addTab("Visual", "👁️", 3)
local hTab = addTab("Hunt", "🍎", 4)

addToggle(fTab, "Auto Level (Fast)", 1, function(v) print("Farm: " .. tostring(v)) end)
addToggle(pTab, "Silent Aim (99%)", 1, function(v) Ghost_Config.SilentAim = v end)
addToggle(pTab, "Auto Clicker", 2, function(v) Ghost_Config.AutoClick = v end)
addToggle(vTab, "Player Box ESP", 1, function(v) Ghost_Config.ESP_Box = v end)

-- ===== CORE ENGINE (FIXED & SUPREME) =====

-- 1. Silent Aim (Metatable Hook) - Bạn yêu cầu check kỹ character
local mt = getrawmetatable(game)
local old = mt.__index
setreadonly(mt, false)
mt.__index = newcclosure(function(t, k)
    if t == mouse and (k == "Hit" or k == "Target") and Ghost_Config.SilentAim and Ghost_Config.Target then
        local char = Ghost_Config.Target.Character
        if char and char:FindFirstChild("HumanoidRootPart") and char.Humanoid.Health > 0 then
            local p = char.HumanoidRootPart.CFrame + (char.HumanoidRootPart.Velocity * Ghost_Config.Prediction)
            return (k == "Hit" and p or char.HumanoidRootPart)
        end
    end
    return old(t, k)
end)

-- 2. Smart Target Loop
task.spawn(function()
    while task.wait(0.2) do
        local near = nil
        local d = 600
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character.Humanoid.Health > 0 then
                local dist = (player.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
                if dist < d then d = dist near = p end
            end
        end
        Ghost_Config.Target = near
        
        -- Auto Click
        if Ghost_Config.AutoClick and Ghost_Config.Target and d < 40 then
            game:GetService("VirtualUser"):ClickButton1(Vector2.new(0,0))
        end
    end
end)

-- 3. ESP (Oracle System)
local function applyESP(p)
    local box = Drawing.new("Square")
    box.Visible = false
    box.Color = Color3.fromRGB(255, 0, 0)
    box.Thickness = 1.5
    
    RunService.RenderStepped:Connect(function()
        if Ghost_Config.ESP_Box and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p ~= player then
            local pos, onScreen = camera:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
            if onScreen then
                box.Size = Vector2.new(1000/pos.Z, 1500/pos.Z)
                box.Position = Vector2.new(pos.X - box.Size.X/2, pos.Y - box.Size.Y/2)
                box.Visible = true
            else box.Visible = false end
        else box.Visible = false end
    end)
end
for _, p in pairs(Players:GetPlayers()) do applyESP(p) end
Players.PlayerAdded:Connect(applyESP)

print("👿 GHOST REDZ V1 LOADED! NO KEY, NO BUGS. 🚀")